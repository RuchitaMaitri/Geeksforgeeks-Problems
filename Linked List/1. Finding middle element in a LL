def findMid(head):
    # Code here
    # return the value stored in the middle node
    p=head
    q=head
    while(q!=None and q.next!=None):
        p=p.next
        q=q.next.next
        
    return p.data
